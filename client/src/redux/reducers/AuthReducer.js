const initState = {
    loading:false,
    registerError:[],
    loginError:[],
    token:'',
    user:''
} 

const AuthReducer = (state= initState,action) => {
    return state;
}

export default AuthReducer;